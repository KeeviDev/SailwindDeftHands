namespace DeftHands.Configuration
{
    /// <summary>
    /// Strength of rotation weight simulation. Both presets use the same smoothing curve and
    /// differ only in smooth time.
    /// </summary>
    public enum RotationWeightPreset
    {
        Standard,
        Light
    }
}
